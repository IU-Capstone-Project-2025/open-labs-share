 Environment variables for Feedback Service
# Copy this file to .env and modify as needed

# Server configuration
GRPC_PORT=9090

# Database configuration (PostgreSQL for feedback metadata)
DB_HOST=localhost
DB_PORT=5432
DB_USER=feedback_user
DB_PASSWORD=feedback_password
DB_NAME=feedback_db

# MongoDB configuration (for comments and feedback content)
MONGODB_URI=mongodb://localhost:27017
MONGODB_DATABASE=feedback
MONGODB_COLLECTION=comments

# MinIO configuration
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET_NAME=feedback
MINIO_USE_SSL=false
MINIO_CREATE_BUCKET=true